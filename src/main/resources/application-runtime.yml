contentgrid:
  gateway:
    runtime-platform:
      enabled: true

testing:
  bootstrap:
    enable: false

spring:
  main:
    cloud-platform: kubernetes # bypass detection this is running on K8S

servicediscovery:
  namespace: default
  enabled: true
